<%= form_for(@pharmacy) do |f| %>
  <% if @pharmacy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pharmacy.errors.count, "error") %> prohibited this pharmacy from being saved:</h2>

      <ul>
      <% @pharmacy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.text_field :name, class:"form-control no-border-radius", autocomplete: "off", placeholder:"Name" %>
  </div>

  <div class="field">
    <%= f.text_field :number, class:"form-control no-border-radius", autocomplete: "off", placeholder:"Phone Number" %>
  </div>
  <div class="field">
    <%= f.text_field :street, class:"form-control no-border-radius", autofocus: true, placeholder:"Street Address" %>
  </div>
  <div class="actions">
    <%= f.submit "Update!", class:"btn btn-success btn-block no-border-radius driver-signup-button"%>
  </div>
<% end %>
