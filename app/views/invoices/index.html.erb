<style>
    .disabled {
        color: #e1e1e2 !important;
    }
    .top-panel {
        border-bottom: 1px solid #e1e1e2;
        padding-top: 20px;
        padding-bottom: 20px;
    }
    .transaction-buttons {
        font-size: 12px;
        padding: 5px;
    }
    .ellipsis {
        background-color: transparent;
    }
    .ellipsis:hover {
        background-color: transparent;
        border: 1px solid #e1e1e2;
        border-radius: 2px;
        padding-right: 4px;
        padding-left: 4px;
        cursor: pointer;
    }
    .table-header {
        border-top: 1px solid #e1e1e2;
        border-bottom: 1px solid #e1e1e2;
        padding-top: 10px;
        padding-bottom: 10px;
        background-color: #fafbfc;
        color: #707071;
        font-size: 15px;
        font-weight: 900;
    }
    #placeholder {
        padding-top: 50px;
        padding-bottom: 50px;
        opacity: 0.5;
    }
    .fa-credit-card {
        font-size: 40px;
    }
    .show-charge-link {
        cursor: pointer;
    }
    #charge {
        border-bottom: 1px solid #e1e1e2;
        padding-top: 10px;
        padding-bottom: 10px;
        font-size: 15px;
        font-weight: 500;
        color: #707071;
    }
    #placeholder, #placeholder-fail, #all-charges, .spinner-row {
        display: none;
    }
</style>

<%= render 'common/spinner' %>
<div id="main">
    <header class="top-panel row">
        <div class="col-md-1 top-panel-div">
            <div class="dropdown">
              <button class="btn btn-secondary dropdown-toggle transaction-buttons" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Filter
              </button>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <%= render 'dropdown' %>
              </div>
            </div>
        </div>
        <div class="col-md-1 offset-md-10 top-panel-div">
              <button class="btn btn-secondary dropdown-toggle transaction-buttons" type="button" id="dropdownMenuButton1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Export
              </button>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                <a class="dropdown-item" href="#">Action</a>
                <a class="dropdown-item" href="#">Another action</a>
                <a class="dropdown-item" href="#">Something else here</a>
              </div>
        </div>
    </header>
    
    <section id="placeholder" class="row">
        <div class="col-md-6 offset-md-3 text-center">
            <i class="fa fa-credit-card"></i>
            <h6>You have no charges</h6>
        </div>
    </section>
    
    <section id="placeholder-fail" class="row">
        <div class="col-md-6 offset-md-3 text-center">
            <i class="fa fa-frown"></i>
            <h6>This page could not be loaded</h6>
            <a class="btn btn-primary reload-button" href=""><i class="fa fa-sync"></i> Reload</a>
        </div>
    </section>
    
    
    <section id="all-charges" class="row">
        <div class="col-md-12 table-header">
            <div class="row">
                <div class="col-md-1 no-right-padding">
                    <span></span>
                </div>
                <div class="col-md-1 no-horizontal-padding">
                    <span>Amount</span>
                </div>
                <div class="col-md-1 no-horizontal-padding">
                    <span></span>
                </div>
                <div class="col-md-4 no-horizontal-padding">
                    <span>Description</span>
                </div>
                <div class="col-md-3 offset-md-1 no-horizontal-padding">
                    <span>Date</span>
                </div>
                <div class="col-md-1 no-horizontal-padding">
                    <span></span>
                </div>
            </div>
        </div>
        <div id="invoices">
            <%= render invoice_sort %>
        </div>
    </section>
</div>

<script>
    $('.spinner-row').show();
    var charge = document.getElementById("charge")
    if (charge) {
        $('.spinner-row').hide();
        $('#all-charges').css('padding-bottom', '100px');
        $('#all-charges').show();
    } else {
        $('.spinner-row').hide();
        $('#placeholder').show();
    };
    
    $('.show-charge-link').on('click', function() {
        $('#main').hide();
        $('.spinner-row').show();
    });
    
    $('.item').on('click', function() {
        $('#main').hide();
        $('.spinner-row').show();
    });
</script>